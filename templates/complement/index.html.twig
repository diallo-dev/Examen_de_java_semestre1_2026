{% extends 'base.html.twig' %}
{% block title %}Gestion Compléments{% endblock %}
{% block body %}
<h1 class="mb-4"><i class="fas fa-glass"></i> Gestion des Compléments</h1>

<div class="table-responsive">
    <table class="table table-hover">
        <thead class="table-danger">
            <tr>
                <th>Image</th>
                <th>Nom</th>
                <th>Prix</th>
                <th>État</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            {% for complement in complements %}
                <tr>
                    <td>
                        {% if complement.urlImage %}
                            <img src="{{ complement.urlImage }}" style="width:50px;height:50px;object-fit:cover;" class="rounded">
                        {% else %}
                            <div class="bg-secondary text-white d-flex align-items-center justify-content-center rounded" style="width:50px;height:50px;">
                                <i class="fas fa-glass"></i>
                            </div>
                        {% endif %}
                    </td>
                    <td>{{ complement.nom }}</td>
                    <td>{{ complement.prix|number_format(0, ',', ' ') }} FCFA</td>
                    <td>
                        <span class="badge bg-{{ complement.etatstock == 'disponible' ? 'success' : (complement.etatstock == 'archiver' ? 'secondary' : 'warning') }}">
                            {{ complement.etatstock }}
                        </span>
                    </td>
                    <td>
                        <a href="{{ path('complement_edit', {id: complement.id}) }}" class="btn btn-sm btn-primary">
                            <i class="fas fa-edit"></i> Modifier
                        </a>
                        {% if not complement.isArchive %}
                            <form method="post" action="{{ path('complement_archive', {id: complement.id}) }}" style="display:inline;" 
                                  onsubmit="return confirm('Archiver ce complément ?');">
                                <button type="submit" class="btn btn-sm btn-secondary">
                                    <i class="fas fa-archive"></i> Archiver
                                </button>
                            </form>
                        {% endif %}
                    </td>
                </tr>
            {% else %}
                <tr><td colspan="5" class="text-center text-muted">Aucun complément</td></tr>
            {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
